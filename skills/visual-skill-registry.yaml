{
  "schema_version": 1,
  "deprecated": true,
  "canonical_source": "skills/visual-skill-registry.json",
  "note": "Runtime tools read visual-skill-registry.json. This JSON-compatible YAML file remains only for older references."
}
