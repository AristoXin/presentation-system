{
  "schema_version": 1,
  "source_of_truth": "skills/visual-skill-registry.json",
  "skills": [
    {
      "id": "presentation-system",
      "role": "lightweight-router-production-governance",
      "required": "required",
      "source": ".",
      "install": "project-local",
      "verify": "test -f SKILL.md && test -f references/index.yaml"
    },
    {"id": "ppt-master", "required": "conditional", "install": "local", "required_when": "pptx_editable"},
    {"id": "frontend-slides", "required": "conditional", "install": "local", "required_when": "html_interactive"},
    {"id": "html-ppt", "required": "optional", "install": "local", "required_when": "html_deck"},
    {"id": "design-dna", "required": "optional", "install": "local", "required_when": "design identity is required"},
    {"id": "guizang-ppt-skill", "required": "optional", "install": "local", "required_when": "guizang route is required"},
    {"id": "taste-skill", "required": "optional", "install": "local", "required_when": "aesthetic or anti-template preflight is required"},
    {"id": "stylekit-skill", "required": "optional", "install": "local", "required_when": "style candidate exploration is required"},
    {"id": "huashu-design", "required": "optional", "install": "local", "required_when": "high-fidelity design variants are required"},
    {"id": "huashu-slides", "required": "optional", "install": "local", "required_when": "image-deck or brand-asset protocol is required"},
    {"id": "humanize-ppt", "required": "optional", "install": "local", "required_when": "narrative AST or page briefs are required"},
    {
      "id": "last30days-search",
      "required": "conditional",
      "install": "public-github",
      "repository": "https://github.com/mvanhorn/last30days-skill",
      "required_when": "recent visual trend or current community signal research is required"
    },
    {
      "id": "headroom",
      "required": "recommended",
      "install": "embedded",
      "source": "skills/vendor/headroom",
      "verify": "test -f skills/vendor/headroom/SKILL.md"
    }
  ],
  "online_toolchain": [
    {"id": "tavily-search", "required": "conditional", "install": "requires-user-consent-and-api-key-or-connector"},
    {"id": "brave-search", "required": "conditional", "install": "requires-user-consent-and-api-key-or-connector"},
    {"id": "firecrawl", "required": "conditional", "install": "requires-user-consent-and-api-key-or-connector"},
    {"id": "context7", "required": "conditional", "install": "requires-user-consent-or-connector"},
    {"id": "playwright-mcp", "required": "conditional", "install": "requires-user-consent-or-local-mcp"}
  ],
  "capabilities": [
    {"id": "subagent", "required": "conditional", "verify": "requires-live-tool-check", "blocker_gate": "G5"},
    {"id": "browser", "required": "conditional", "verify": "requires-live-browser-check", "blocker_gate": "G7"},
    {"id": "visual-qa", "required": "conditional", "verify": "requires-screenshot-or-equivalent-evidence", "blocker_gate": "G7"}
  ]
}
